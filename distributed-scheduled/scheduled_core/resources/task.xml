<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="
            http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd
            http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.1.xsd">
	<!-- // DefaultListableBeanFactory acf = (DefaultListableBeanFactory) // 
		ac.getAutowireCapableBeanFactory(); // Resource res = new ClassPathResource("task.xml"); 
		// DefaultListableBeanFactory factory = new // DefaultListableBeanFactory(); 
		// XmlBeanDefinitionReader reader = new // XmlBeanDefinitionReader(factory); 
		// reader.loadBeanDefinitions(res); // acf.registerBeanDefinition("contextLifecycleScheduledTaskRegistrar", 
		// reader.getBeanFactory().getBeanDefinition("contextLifecycleScheduledTaskRegistrar")); 
		// ZookeeperScheduledTaskRegistrar zookeeperScheduledTaskRegistrar = ac // 
		.getBean(ZookeeperScheduledTaskRegistrar.class); // zookeeperScheduledTaskRegistrar 
		// .addCronTask(new ScheduledMethodRunnable(ac.getBean("zookeeperTask"), 
		// "autoChecked"), "0 * * * * *"); // zookeeperScheduledTaskRegistrar.reloadTask(); 
		// zookeeperScheduledTaskRegistrar.destroy(); -->
</beans>